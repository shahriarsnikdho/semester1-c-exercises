#include <stdio.h>
// area of square
int main()
{

    int side;

    printf("Enter side of square:");
    scanf("%d", &side);

    printf("Area of the square is %d", side*side);

    return 0;
}