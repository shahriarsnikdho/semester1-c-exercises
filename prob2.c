#include <stdio.h>
// area of circle
int main()
{

    float radius;
    float pi = 3.1416;

    printf("Enter radius of circle:");
    scanf("%f", &radius);

    printf("Area of the square is %f", pi*radius*radius);

    return 0;
}